# Secrets for local Kamal deploys. In GitHub Actions these are injected via workflow env vars.
# DO NOT commit raw credentials here.

RAILS_MASTER_KEY="${RAILS_MASTER_KEY:-$(cat config/master.key)}"
KAMAL_REGISTRY_PASSWORD="${KAMAL_REGISTRY_PASSWORD}"
